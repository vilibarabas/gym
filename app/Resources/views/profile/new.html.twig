{% extends 'base.html.twig' %}

{% block body %}
    <h1>Profile creation</h1>
    <a href="{{ path('personal_evolutions') }}">Evolutions</a>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('profile_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
